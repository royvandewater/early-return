{
  "name": "early-return",
  "version": "1.0.0",
  "description": "Language that abolishes if conditionals, leverages early returns, and uses events a lot",
  "main": "index.js",
  "scripts": {
    "start": "mocha --compilers coffee:coffee-script/register --require coffee-script --require test/test_helper.coffee --recursive --growl --reporter spec --growl --watch",
    "test": "mocha --compilers coffee:coffee-script/register --require coffee-script --require test/test_helper.coffee --recursive --growl --reporter spec"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/royvandewater/early-return.git"
  },
  "keywords": [
    "language",
    "conditional",
    "events"
  ],
  "author": "Roy van de Water",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/royvandewater/early-return/issues"
  },
  "homepage": "https://github.com/royvandewater/early-return",
  "devDependencies": {
    "chai": "^1.9.2",
    "mocha": "^1.21.4",
    "sinon": "^1.10.3",
    "sinon-chai": "^2.5.0"
  },
  "dependencies": {
    "coffee-script": "^1.8.0",
    "jison": "^0.4.15",
    "lodash": "^2.4.1"
  }
}
